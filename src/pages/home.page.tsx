import LeftNavBar from "../components/left.nav.bar";

const HomePage = () => {
  return (
    <div className="w-full h-screen bg-gray p-10">
      <LeftNavBar />
    </div>
  );
};

export default HomePage;
